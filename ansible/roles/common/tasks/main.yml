---

- name: Update apt packages
  become: yes
  ansible.builtin.apt:
    update_cache: true

- name: Install common packages
  become: yes
  ansible.builtin.apt:
    name:
      - curl
      - wget
      - git
      - snapd

- name: Link .gitconfig
  ansible.builtin.file:
    src: "{{ dotfiles }}/git/.gitconfig"
    dest: "{{ home }}/.gitconfig"
    state: link
