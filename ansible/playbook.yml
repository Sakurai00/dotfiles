---
- name: Playbook
  hosts: localhost
  connection: local

  vars_files:
    - "vars/env.yml"

  roles:
    - role: common
    - role: cli_tools
    - role: zsh
      tags: zsh
    - role: rust
      tags: rust
